# Privy App ID (required for authentication)
# Get this from https://dashboard.privy.io after creating an app
VITE_PRIVY_APP_ID=your_privy_app_id_here

# API URL (optional, defaults to proxy in vite.config.js)
# Only needed if you want to override the default proxy configuration
# VITE_API_URL=http://localhost:5000
